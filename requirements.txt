# Core dependencies for Color Transfer Framework v2.0

# Required
numpy>=1.20.0
opencv-python>=4.5.0
psutil>=5.8.0  # For performance profiling

# Interface Layer dependencies
typer>=0.9.0  # CLI
rich>=13.0.0  # CLI formatting
fastapi>=0.100.0  # REST API
uvicorn>=0.23.0  # ASGI server
gunicorn>=21.2.0  # WSGI server for Flask production
python-multipart>=0.0.6  # File uploads in FastAPI
requests>=2.31.0  # HTTP client for health checks
pydantic>=2.0.0  # Data validation
flask>=2.3.0  # WebUI
flask-sock>=0.6.0  # WebSocket support for Flask
textual>=0.48.0  # Terminal UI framework
pyyaml>=6.0  # Configuration file support

# Optional (for GPU acceleration)
# torch>=1.9.0
# torchvision>=0.10.0

# Optional (for visualization - required for DiagnosticsVisualizer)
matplotlib>=3.3.0
seaborn>=0.11.0
# plotly>=5.0.0

# Required for advanced processing (palette extraction)
scikit-learn>=0.24.0

# Performance & Scalability (Phase 11)
celery[redis]>=5.3.0  # Distributed task processing with RabbitMQ/Redis backend
redis>=5.0.0  # Distributed caching layer

# Security & Operations (Phase 12)
python-dotenv>=1.0.0  # Environment variable management (.env file support)

# Optional (for API server)
# fastapi>=0.68.0
# uvicorn>=0.15.0

# Optional (for documentation)
# sphinx>=4.0.0
# jupyter>=1.0.0
# nbconvert>=6.0.0
